@model Shop.Mvc.Application.Commands.Producers.EditProducerCommand
@{
    ViewData["Title"] = "Edit producer";
}

<div class="container">
    <div class="container-box">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a asp-area="Panel" asp-controller="Home" asp-action="Index">Panel</a></li>
                <li class="breadcrumb-item"><a asp-area="Panel" asp-controller="Producers" asp-action="Index">Producers</a></li>
                <li class="breadcrumb-item active" aria-current="page">Create</li>
            </ol>
        </nav>

        @if (Model.Saved)
        {
            <div class="alert alert-success" role="alert">
                Producer updated!
            </div>
        }

        <form asp-area="Panel" asp-controller="Producers" asp-action="Edit" method="post">
            <div class="form-group">
                <label for="id">Id</label>
                <input asp-for="Id" type="text" class="form-control" id="id" placeholder="Id" readonly />
            </div>
            <div class="form-group">
                <label for="producerName">Producer name</label>
                <input asp-for="Name" type="text" class="form-control" id="producerName" placeholder="Producer name" />
            </div>
            <div class="form-group">
                <label for="description">Description</label>
                @Html.TextAreaFor(e => e.Description, new { @class = "form-control", @id = "description", placeholder = "Description" })
            </div>
            <button type="submit" class="btn btn-primary">Save</button>
        </form>
    </div>
</div>