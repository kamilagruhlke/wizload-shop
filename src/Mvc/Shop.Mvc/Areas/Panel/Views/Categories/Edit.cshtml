@model Shop.Mvc.Application.Models.CategoryEditModel
@{
    ViewData["Title"] = "Create category";
}

<div class="container">
    <div class="container-box">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a asp-area="Panel" asp-controller="Home" asp-action="Index">Panel</a></li>
                <li class="breadcrumb-item"><a asp-area="Panel" asp-controller="Categories" asp-action="Index">Categories</a></li>
                <li class="breadcrumb-item active" aria-current="page">Create</li>
            </ol>
        </nav>

        @if (Model.Saved)
        {
            <div class="alert alert-success" role="alert">
                Category updated!
            </div>
        }

        <form asp-area="Panel" asp-controller="Categories" asp-action="Edit" method="post">
            <div class="form-group">
                <label for="id">Id</label>
                <input asp-for="EditCategoryCommand.Id" type="text" class="form-control" id="id" placeholder="Id" readonly />
            </div>
            <div class="form-group">
                <label for="categoryId">Parent category</label>
                <select asp-for="EditCategoryCommand.ParentId" class="form-control" id="categoryId">
                    <option selected />
                    @foreach (var category in Model.Categories)
                    {
                        <option value="@category.Id">@category.Name</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <label for="producerName">Category name</label>
                <input asp-for="EditCategoryCommand.Name" type="text" class="form-control" id="categoryName" placeholder="Category name" />
            </div>
            <button type="submit" class="btn btn-primary">Save</button>
        </form>
    </div>
</div>